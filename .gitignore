# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude local settings (never commit — contains local paths)
.claude/settings.local.json

# DAC zip archives — stored separately, not in git
# (Use DAC-STORE-IGNORE/ folder locally, add to .gitignore)
DAC-STORE-IGNORE/

# Editor
.vscode/settings.json
*.swp
*.swo